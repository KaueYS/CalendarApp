@model ContatoModel
@{
    ViewData["Title"] = "apagar o contato";
}

<div class="text-center">

<h3>Deseja Apagar o contato (@Model.Nome)</h3>

</div>

<a class="btn btn-primary" role="button" asp-controller="Contato" asp-action="Index">CANCELAR</a>
<a class="btn btn-primary" asp-route-id="@Model.Id" role="button" asp-controller="Contato" asp-action="Apagar">APAGAR</a>